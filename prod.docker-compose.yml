version: "3.7"
x-production:
  &production-defaults
  image: milo-prod
  ports:
    - "4000:4000"
  environment:
    - NODE_ENV=production
services:
  test:
    build:
      context: ./api
      dockerfile: prod.dockerfile
      target: dependencies
    <<: *production-defaults
  api:
    build:
      context: ./api
      dockerfile: prod.dockerfile
    <<: *production-defaults
